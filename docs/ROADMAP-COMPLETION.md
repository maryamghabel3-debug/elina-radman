# 🗺️ نقشه‌راه تکمیل الینا — چه چیزی زنده است و چه چیزی مانده

این سند بعد از **بازبینی کامل فایل‌به‌فایل** کل پروژه ساخته شده تا هیچ قابلیتی از قلم نیفتد.
هر بخش را «یکی‌یکی» به سرویس واقعی وصل می‌کنیم و اینجا تیک می‌زنیم.

آخرین بازبینی: بعد از راه‌اندازی موفق تولید کپشن (Claude)، ترند، تحلیل عمیق، ربات تلگرام.

---

## ✅ بخش ۱ — کاملاً زنده و کارکننده (تمام‌شده)

| قابلیت | ایجنت/فایل | وضعیت |
|--------|-----------|-------|
| تولید کپشن با AI | `content_creator.py` + `llm_router.py` | ✅ زنده (Claude/OpenRouter + Gemini) |
| ترندیابی واقعی | `trend_hunter.py` | ✅ زنده (Reddit + YouTube + Google Trends) |
| تحلیل عمیق عکس ترند | `trend_visual_analyzer.py` + `vision.py` | ✅ زنده (Gemini Vision) |
| مهندسی معکوس ویدیو | `trend_video_analyzer.py` | ✅ زنده |
| ربات تلگرام | `elina_bot.py` | ✅ زنده (۱۵+ دستور) |
| خط تولید روزانه | `generate.py` + workflow | ✅ زنده |
| تحلیل عملکرد | `performance_analyzer.py` | ✅ زنده (YouTube/IG واقعی + fallback) |
| مدیریت گیت‌هاب | `github_manager.py` | ✅ زنده |
| دفترچه احساسات | bot + `content_creator` | ✅ زنده |

---

## 🟡 بخش ۲ — «اسکلت هست ولی به سرویس واقعی وصل نیست» (کار اصلی ما)

این‌ها کد دارند ولی خروجی‌شان **mock/شبیه‌سازی** است یا **در خط تولید صدا زده نمی‌شوند**.

### 🎨 ۲.۱ — ساخت عکس واقعی الینا
- **فایل:** `platform_managers.py` → `VisualCreatorAgent`
- **وضعیت:** کد InstantID هست ولی: (۱) نیاز به `HF_TOKEN` دارد، (۲) در خط روزانه/تلگرام صدا زده نمی‌شود.
- **کاری که لازم است:** وصل به یک سرویس تولید تصویر رایگان/پایدار + افزودن به خط تولید + دستور `/photo` در تلگرام.

### 🎬 ۲.۲ — ساخت ویدیوی واقعی
- **فایل:** `video_generator.py` → `DirectorAgent`
- **وضعیت:** فقط «مدیر پروژه» تصمیم می‌گیرد؛ همه خروجی‌ها `mock_*.mp4`. یک باگ: `test_media.py` تابع `generate_video_shot` را صدا می‌زند که وجود ندارد.
- **کاری که لازم است:** وصل به یک ابزار faceless-video واقعی (مثل FreeFaceless: script→TTS→زیرنویس→ffmpeg→خروجی) یا HF Space واقعی.

### 💰 ۲.۳ — افیلیت مارکتینگ واقعی
- **فایل:** `product_hunter.py`
- **وضعیت:** لیست محصولات و لینک‌ها ثابت/mock هستند (`mock-trench`, `shopltk.com/mock...`).
- **کاری که لازم است:** لینک‌های واقعی افیلیت (Amazon Associates / LTK / ShopStyle) + تزریق درست در کپشن‌ها با CTA.

### 👗 ۲.۴ — پیدا کردن لباس واقعی
- **فایل:** `fashion_stylist.py` + `product_hunter.py`
- **وضعیت:** moodboard ثابت + محصولات ثابت (اسکرپ واقعی از فروشگاه‌ها ندارد).
- **کاری که لازم است:** اتصال به یک منبع واقعی محصول (API/فید افیلیت) یا حداقل جستجوی واقعی.

### 📦 ۲.۵ — دراپ‌شیپینگ / برند اختصاصی
- **مستند:** `BUSINESS-PLAN.md` (ShineOn جواهرات، Pietra برند لباس)
- **وضعیت:** فقط اسم برده شده؛ اتصال واقعی ندارد.
- **کاری که لازم است:** تصمیم تجاری + ثبت‌نام سرویس + اتصال (این بیشتر کار دستی/تجاری است تا کد).

---

## 🔊 بخش ۳ — قابلیت‌های وعده‌داده‌شده در اسناد که هنوز کد ندارند

| قابلیت | منبع سند | وضعیت |
|--------|----------|-------|
| صداگذاری (Voiceover / Edge-TTS / ElevenLabs) | چند سند + requirements | ⚠️ `edge-tts` در requirements هست ولی استفاده نمی‌شود |
| زیرنویس خودکار (Whisper) | FACTORY / اسناد ویدیو | ❌ کد ندارد |
| ساخت تصویر با Leonardo AI | README (۱۵۰ عکس/روز رایگان) | ❌ کد ندارد (جایگزین HF است) |
| تولید Thumbnail + A/B تست | FACTORY-ARCHITECTURE | ❌ کد ندارد |
| استراتژی چندزبانه (فارسی/انگلیسی) | `MULTI-LANGUAGE-STRATEGY.md` | ⚠️ فقط چت فارسی است؛ محتوای دوزبانه خودکار نیست |
| محصولات دیجیتال (ای‌بوک) | BUSINESS-PLAN | ❌ کد ندارد (کار محتوایی) |
| Linktree / bio CTA | BUSINESS-PLAN | ❌ خودکار نیست |
| پلتفرم‌های Lemon8 / Snapchat / FB Reels | BUSINESS-PLAN | ⚠️ در لیست platforms هست ولی publisher واقعی ندارد |
| کارخانه یوتیوب (چند-کانال) | `FACTORY-ARCHITECTURE.md` | ❌ فقط سند (پروژه آینده) |

---

## 🔧 بخش ۴ — باگ‌ها/بدهی فنی که حین بازبینی پیدا شد

- [ ] `test_media.py` → `DirectorAgent.generate_video_shot()` را صدا می‌زند که **وجود ندارد** (متد فعلی `run_managed_project` است).
- [ ] ایجنت‌های `ProductHunter`, `FashionStylist`, `VisualCreator`, `DirectorAgent` **در هیچ خط تولید خودکاری صدا زده نمی‌شوند** (فقط تست دستی).
- [ ] `mcp_server.py` قدیمی است و ابزارهای جدید (تحلیل عمیق، ویدیو) را ندارد.
- [ ] داشبورد بخش Media Studio هنوز به تولید واقعی وصل نیست.

---

## 📋 ترتیب پیشنهادی اتصال (یکی‌یکی)

۱. 🎨 **ساخت عکس** (پایه‌ی همه‌چیز — بدون عکس، پست فشن معنا ندارد)
۲. 💰 **افیلیت واقعی** (درآمدزایی — سریع‌ترین ارزش تجاری)
۳. 👗 **پیدا کردن لباس واقعی** (تغذیه‌کننده‌ی افیلیت و عکس)
۴. 🎬 **ساخت ویدیو** (پیچیده‌ترین — Reels/Shorts/TikTok)
۵. 🔊 **صداگذاری + زیرنویس** (مکمل ویدیو)
۶. 📦 **دراپ‌شیپینگ/برند** (تصمیم تجاری)
۷. 🏭 **کارخانه یوتیوب** (بلندمدت)

> هر بخش که تمام شد، اینجا از 🟡 به ✅ منتقل و در بخش ۱ ثبت می‌شود.
