services:
  - type: web
    name: elina-bots
    env: python
    buildCommand: "bash scripts/setup_render_environment.sh && pip install -r requirements-core.txt"
    startCommand: "python scripts/run_server.py"
