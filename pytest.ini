[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    unit: fast isolated unit tests
    integration: tests involving multiple internal components
    smoke: critical import and startup smoke tests
    slow: tests excluded from the default CI run
